```rust
use serde::Deserialize;

#[derive(Debug, Deserialize)]
struct Dialogue {
    character: String,
    text: String,
    portrait: String,
    
    #[serde(default)]
    bgm: Option<String>,
    
    #[serde(default)]
    background: Option<String>,
}