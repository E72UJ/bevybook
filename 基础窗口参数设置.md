```rust
        .add_plugins(DefaultPlugins.set(WindowPlugin {
            primary_window: Some(Window {
                name: Some("raven.app".into()),
                resizable: false,
                enabled_buttons: bevy::window::EnabledButtons {
                    maximize: true,
                    ..Default::default()
                },
                resolution: (1400, 720).into(),
                ..default()
            }),
            ..default()
        }))
```